name: Test
on: push
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
      - run: pip install hermes-pmxt 2>&1 | tail -3
      - env:
          K: ${{ secrets.POLY_PRIVATE_KEY }}
        run: python -c "import os; print('key' if os.environ.get('K') else 'no')"
